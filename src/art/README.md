# `src/art/` — Agent 3

Local Met Open Access catalog and canvas painter.

- Source images from [The Met Open Access](https://metmuseum.github.io/). Bundle files under `public/artworks/`.
- Do **not** fetch images or metadata at runtime.
- Each `Artwork` must include a `description` fun fact / context string.
