# `src/ui/` — Agent 4

Controls and copy. Uses `src/shared/` types only; no Tauri APIs.

Planned files: `Settings.tsx`, `StudyChip.tsx`, `BreakComplete.tsx`.

BreakComplete must show **title, artist, year, and description** (fun fact / context).
