# `src-tauri/` — Agent 1

Tauri window shell: compact study chip, fullscreen break, click-through dissolve.

Starting window in `tauri.conf.json` is frameless and always-on-top. Refine modes (resize, fullscreen, `ignoreCursorEvents`) here and in `src/app/windowBridge.ts`.
